<h1>Hello Message</h1>
<%= link_to 'new', new_message_path,class: "btn btn-primary btn-block" %>
<% @messages.each do |message| %>
    <h3><%=message.title %></h3>
    <p><%=message.description %></p>
     <%= link_to 'show', message_path(message),class: "btn btn-info" %>
<% end %>
